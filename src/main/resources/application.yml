spring:
  datasource:
    url: jdbc:postgresql://179.0.10.25:5432/supplychain
    username: scms
    password: $upplyChæ!n

    jpa:
      hibernate:
        ddl-auto: update
      database: postgresql
      database-platform: org.hibernate.dialect.PostgreSQLDialect

  logging:
    level:
      org.hibernate.SQL: debug
      org:
        hibernate:
          type: trace